fri_state_broadcaster:
  robot_prefixes:
    type: string_array
    # Keep one entry by default; empty string means unprefixed single-robot behavior.
    default_value: [""]
    description: List of robot prefixes used for binding FRI state interfaces.
