kuka_kss_message_handler:
  robot_names:
    type: string_array
    # Keep one entry by default; empty string means unprefixed single-robot behavior.
    default_value: [""]
    description: List of robot prefixes used for binding KSS state/command interfaces.
